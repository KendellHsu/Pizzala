# Problem.md

> 目前可以改善的問題。發現新問題就記在這裡；誰要動工修，就照 [WORKFLOW.md](WORKFLOW.md) 開功能分支交付。




## 2. 醬料的噴濺
目前醬料是一張平面的貼圖貼在撞擊點上，但如果貼在人物或是斜面上的話，會看起來很奇怪(一片醬料懸浮在空中)，我希望醬料可以更像液體一點，會潑灑在地面上

## 3. 角色的生成跟移動 ✅ 已處理
目前的角色還沒辦法移動，角色生成的機制也還沒加入。
在 Commit e117360 中，原本有角色的情緒加速機制跟生成機制。

**處理紀錄(2026-07-15)**：已從舊版原型移植回來——
- `CustomerController` 加回三段情緒(耐心→不耐煩→暴躁)，等餐越久遊走越快，移動時自動切換走路動畫；門檻與速度在 `ThrowTuning` 的「客人情緒加速」區塊調整。
- 新增 `CustomerSpawner`(槽位制：左/中/右扇區 × 距離層)，回合中動態生成客人，訂單結束或閒置超時就離場讓位；生成節奏同樣在 `ThrowTuning` 調整。
- `GameManager` 改為動態客人清單，生成的客人自動進訂單池。

